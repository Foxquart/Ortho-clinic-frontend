/**
 * Placeholder — the real animated landing is built by the landing agents.
 * Kept minimal so the router typechecks in the meantime.
 */
export function LandingPage() {
  return <div className="bg-bg text-text min-h-screen" />
}
